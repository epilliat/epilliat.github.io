/.quarto/
**/speaker_notes/
*.draft.qmd

**/corrections/

# Local-only bandits replay frames: symlinks to ~/bandits/replay, never committed.
# The deployed deck degrades to a link-out; frames are shown only when presenting locally.
**/bandits_many_arms/replay
# v2 talk sandbox (experimental, not version-controlled)
research/presentations/sandbox/
docs/research/presentations/sandbox/
