{
    "cSpell.enabledFileTypes": {
        "quarto": false,
        "julia": false
    },
    "//": "The bandits_many_arms/replay folders are symlinks to ~/bandits/replay (hundreds of SVGs). Exclude them from the explorer, search, and especially the file watcher so VS Code never indexes them (indexing 600+ SVGs froze the machine).",
    "files.exclude": {
        "**/bandits_many_arms/replay": true
    },
    "search.exclude": {
        "**/bandits_many_arms/replay/**": true
    },
    "files.watcherExclude": {
        "**/bandits_many_arms/replay/**": true
    }
}
